import { sharedRules } from '../shared'

export const productRules = {
  required: sharedRules.required,
  codigo: sharedRules.codigo,
  nome: sharedRules.nome,
  alfanumerico: sharedRules.alfanumerico,
  positive: sharedRules.positive,
  numeric: sharedRules.numeric,
}


